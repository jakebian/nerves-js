(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD globals
        define([], factory);
    } else {
        // Browser globals
        root.nerves = factory();
    }
}(this, function () {


